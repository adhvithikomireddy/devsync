{
  "name": "devsync-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "DevSync - Collaborative Engineering Workspace",
  "scripts": {
    "dev": "concurrently \"npm --prefix server start\" \"npm --prefix client run dev\"",
    "server": "npm --prefix server start",
    "client": "npm --prefix client run dev",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install --include=dev",
    "build": "npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "mongodb-memory-server": "^11.2.0"
  }
}
